var React = require("react/addons");

var CatalogItem = React.createClass({
    render: function () {
        return (
            null            
        );
    }
});

module.exports = CatalogItem;

