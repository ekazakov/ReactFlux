var React = require("react/addons");
var Router = require('react-router-component');

var Link = Router.Link;

var CurtSummary = React.createClass({
    render: function () {
        return (
            <div>
                <Link href="/cart" className="btn btn-success">
                    Cart Items: QTY / $COST
                </Link>    
            </div>
        );
    }
});

module.exports = CurtSummary;

